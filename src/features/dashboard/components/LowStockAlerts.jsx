import { Link } from 'react-router'
export default function LowStockAlerts({products}) {
 return <section className="rounded-xl border border-outline p-6"><div className="mb-5 flex items-center justify-between gap-3"><h2 className="text-xl font-bold">Low Stock Alert</h2><Link to="/admin/products?stock=low" className="text-sm text-accent">View All</Link></div><div className="space-y-4">{products.slice(0,3).map(p=><article key={p.id} className="rounded-xl bg-background p-4"><h3 className="font-semibold">{p.name}</h3><p className="mt-2 text-sm text-muted">SKU: {p.sku}</p><div className="mt-4 mb-2 flex justify-between gap-2 text-xs"><span className="font-medium text-red-500">Stock: {p.stock}</span><span className="text-muted">Reorder: {p.reorder}</span></div><div role="meter" aria-label={p.name+' stock relative to reorder level'} aria-valuemin={0} aria-valuemax={Math.max(p.reorder,1)} aria-valuenow={p.stock} className="h-2 overflow-hidden rounded-full bg-avatar"><div className="h-full rounded-full bg-red-500" style={{width:Math.min(100,p.stock/Math.max(p.reorder,1)*100)+'%'}}/></div></article>)}{!products.length&&<p className="py-10 text-center text-sm text-muted">No products are at or below their reorder level.</p>}</div></section>
}
