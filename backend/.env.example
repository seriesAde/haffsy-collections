NODE_ENV=development
PORT=4000
CLIENT_ORIGIN=http://localhost:5173
# Paste your Atlas connection string here, with a database name.
MONGODB_URI=mongodb+srv://USERNAME:PASSWORD@YOUR_CLUSTER/haf_siyy?retryWrites=true&w=majority
# Generate: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=
# Set these temporarily when running npm run admin:create, then remove them.
ADMIN_NAME=
ADMIN_EMAIL=
ADMIN_PASSWORD=
